**带⚠️标记的是关键点，请务必遵守**

当用户指示为指定包指定顶级声明的公共成员生成代码时，按照提供的必要资料生成代码。
    - **必要资料**：《详细设计文档》、《{<codelang>}》、工具库{<toolSummaries>}、{<packageStructure>}、{<declarations>}
    - **代码风格**：简洁高效、高可复用性、高可读性、高可维护性、高可扩展性、高内聚低耦合
    - 如有必要可以
        - 为指定顶级声明添加非公共成员
        - 添加新的顶级声明，添加的顶级声明可以在提供的任意包目录内
        - 在提供的任意包目录任意顶级声明添加新的公共成员
        - 为需要的代码文件修改导入，包括添加、删除等
        为当前模块添加依赖
    - 若对用户请求相关的内容或代码库结构不确定，请使用工具读取文件以收集相关信息，切勿猜测或捏造回复
    - ⚠️如有必要请调用
        - **select函数**查阅《{<codelang>}标准库》
        - **select函数**查阅工具库
        - **search函数**搜索相关信息
            - ⚠️⚠️ 调用**remember函数**将搜索到的信息摘要记忆为文章（'ARTICLE'）
---

## 响应格式

- 如果缺少必要资料
```json
{
    "necessary":["缺省的必要资料名称", "一份资料一个字符串"]
}
```

- 生成的代码
使用JSON数组的格式返回声明，一个代码文件一个JSON对象，包含以下字段：
```json
[{
    "packageName":"path/of/package_name",//创建单元测试的文件所在包目录
    "codeFileName":"顶级声明的文件名",//按照指定语言的格式命名，如top_level_declaration.py、top_level_declaration.go、top_level_declaration.ts、top_level_declaration.cj TopLevelDeclaration.java
    "implementation":"..."//具体实现的代码
}]
```

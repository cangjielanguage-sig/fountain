# fleet 优化方案

---

## 2026-05-04 — 首次全量诊断

审查范围：`fleet/src/` 全部 17 个源文件（25 个文件含测试）
审查方式：静态代码分析

### ~~[P0] asyncPublish 为空桩，节点间数据永不同步~~ **已修复** (ab3ada18)

- 文件：`SyncHandler.cj:199-201`
- 问题：调用 `nodeManager.allOthers()` 但不发送 PUBLISH 消息
- 方案：遍历所有其他节点，spawn 异步任务创建临时 Client 发送 PUBLISH
- 难度：高 → **已完成**

### ~~[P0] NodeManager 健康检查为空桩~~ **已修复** (157361c8)

- 文件：`NodeManager.cj:33-41`
- 问题：spawn 线程每 5 秒无条件 `alive.store(nodeCount)`，无实际心跳
- 方案：单节点不启动；多节点每5秒遍历 allOthers() 发送 PING，2秒超时，响应计数
- 难度：高 → **已完成**

### ~~[P0] WatchManager 后台线程无法停止~~ **已修复** (96d9597d)

- 文件：`WatchManager.cj:88-94`
- 问题：`while(true)` 无退出信号，close() 后线程仍在运行
- 方案：实现 `Resource <: close()/isClosed()`，`AtomicBool` 控制线程退出
- 难度：中 → **已完成**

### ~~[P1] SyncClient.getInstance 非线程安全~~ **已修复** (85f8f390)

- 文件：`SyncClient.cj:41-43`
- 方案：`Mutex` + `synchronized` 保护检查+构造
- 难度：低 → **已完成**

### ~~[P1] SyncServer.start 的 spawn 不捕获异常~~ **已修复** (85f8f390)

- 文件：`SyncServer.cj:37-41`
- 方案：try-catch 包裹，log.error 输出异常
- 难度：低 → **已完成**

### ~~[P1] HashRing.getNextNode 空环异常~~ **已修复** (85f8f390)

- 文件：`HashRing.cj:59`
- 方案：入口检查 `ring.size==0`，if-let 替代 getOrThrow
- 难度：低 → **已完成**

### ~~[P1] exit(1) 不释放资源~~ **已修复** (85f8f390)

- 文件：`SyncCommand.cj:97`
- 方案：exit 前依次 close store/server/watchManager
- 难度：中 → **已完成**

### ~~[P2] notify 过期检查竞态~~ **分析确认无实际竞态** (604dd0df)

- 文件：`WatchManager.cj:183-187`
- 分析：WatchEntry 是 struct，`matched.add(entry)` 时 expiry 已值拷贝到 matched 列表
- 结论：无需修改

### ~~[P2] 主线程忙等~~ **保留** (604dd0df)

- 文件：`SyncCommand.cj:71-73`
- 分析：无 shutdown 信号场景下 `while(true){sleep(60)}` 为标准服务端模式
- 结论：保留

### ~~[P3] WatchFuture.get()/tryGet() 类型不一致~~ **保留** (604dd0df)

- 文件：`WatchManager.cj:43-48`
- 分析：get() 阻塞至 done 后返回，必不为空；tryGet() 可能返回 None，语义合理
- 结论：保留

### ~~[P3] VERSION_SUFFIX 可移至类内部~~ **已修复** (604dd0df)

- 文件：`SyncHandler.cj:28`
- 方案：改为 `private static let` 成员

### ~~[P3] 仲裁+Owner 检查重复模板~~ **已修复** (604dd0df)

- 文件：`SyncHandler.cj:58-69` 和 `82-93`
- 方案：提取 `checkWriteAccess()` 方法

### ~~[P3] ScanEntry 死代码~~ **已修复** (604dd0df)

- 文件：`SyncDataFormat.cj:117-128`
- 方案：删除未使用的 ScanEntry 类

### ~~[P3] HashRing_test 中 @Assert(!expr) 写法~~ **已验证通过** (604dd0df)

- 文件：`HashRing_test.cj:76`
- 分析：`@Assert(!...)` 已在多处使用且测试通过，无实际兼容问题

---

## 2026-05-04 — 第 2 轮诊断（修复后复查）

审查范围：`fleet/src/` 全部 12 个源文件
审查方式：静态代码分析

### ~~[P3] NodeManager 健康检查 spawn 缺少异常保护~~ **已修复** (31dc6721)

- 文件：`NodeManager.cj:42`
- 方案：while 体外套 try-catch
- 难度：低 → **已完成**

### ~~[P3] SyncClient.prefix/glob 代码重复~~ **已修复** (31dc6721)

- 文件：`SyncClient.cj:110-132`
- 方案：提取私有 scan() 方法
- 难度：低 → **已完成**

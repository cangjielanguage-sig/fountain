# 前置任务
cd f_concurrent

# 任务

把ConcurrentSkipListMap_test.cj的测试用例分成三部分
测试函数名包含stress和concurrent的测试函数保存到同包的新测试文件，命名为ConcurrentSkipListMap_conc_test.cj，类名就是ConcurrentSkipListMap_conc_test。

test_perf_ 开头的测试函数保存到同包的新测试文件，命名为ConcurrentSkipListMap_perf_test.cj，类名就是ConcurrentSkipListMap_perf_test。

其它的测试函数保持不变。新的测试文件包声明和版权声明跟ConcurrentSkipListMap_test.cj一样。
# f_hydra 开发计划

> **版本**: v1.0
> **日期**: 2026-05-23
> **目标版本**: v0.1.0 (Linux x86_64 CPU)
> **计划依据**: f_hydra/doc/架构设计文档.md v2.0
> **生成方式**: 3 名架构师 (phase-planner / task-planner / test-strategist) 两轮脑暴 + 交叉评审

---

## 目录

1. [开发阶段总览](#1-开发阶段总览)
2. [关键路径与并行策略](#2-关键路径与并行策略)
3. [Phase 0: 项目脚手架与环境搭建](#3-phase-0-项目脚手架与环境搭建)
4. [Phase 1: FFI 基础设施](#4-phase-1-ffi-基础设施)
5. [Phase 2: 配置、接口与 Tokenizer](#5-phase-2-配置接口与-tokenizer)
6. [Phase 3: OrtCpuBackend 实现](#6-phase-3-ortcpubackend-实现)
7. [Phase 4: 稠密索引 (Dense + HNSW)](#7-phase-4-稠密索引-dense--hnsw)
8. [Phase 5: 稀疏索引 (Sparse + Inverted)](#8-phase-5-稀疏索引-sparse--inverted)
9. [Phase 6: 多向量索引 (ColBERT + IVF/PQ)](#9-phase-6-多向量索引-colbert--ivfpq)
10. [Phase 7: 融合排序层](#10-phase-7-融合排序层)
11. [Phase 8: 引擎集成与并发安全](#11-phase-8-引擎集成与并发安全)
12. [Phase 9: 集成验证与性能基准](#12-phase-9-集成验证与性能基准)
13. [Phase 10: 发布准备](#13-phase-10-发布准备)
14. [依赖关系图](#14-依赖关系图)
15. [风险矩阵](#15-风险矩阵)

---

## 1. 开发阶段总览

| 阶段 | 名称 | 任务数 | 预估工作量 | 是否关键路径 | 可并行 |
|------|------|--------|-----------|-------------|--------|
| P0 | 项目脚手架 + 环境搭建 + Golden 文件 | 3 | S | ✓ 是 | — |
| P1 | FFI 基础设施 | 6 | L | ✓ 是 | — |
| P2 | 配置 + 后端接口 + 共享类型 + Tokenizer | 9 | M | ✓ 是 | — |
| P3 | OrtCpuBackend 实现 | 5 | XL | ✓ 是 | — |
| P4 | 稠密索引 (Dense + HNSW) | 2 | L | ✓ 是 | ∥ P5/P6/P7 |
| P5 | 稀疏索引 (Sparse + Inverted) | 3 | M | — | ∥ P4/P6/P7 |
| P6 | 多向量索引 (ColBERT + IVF/PQ) | 2 | L | — | ∥ P4/P5/P7 |
| P7 | 融合排序层 | 1 | S | — | ∥ P4/P5/P6 |
| P8 | 引擎集成 + 并发安全 | 2 | M | ✓ 是 | — |
| P9 | 集成验证 + 性能基准 | 10 | L | ✓ 是 | — |
| P10 | 发布准备 | 4 | M | ✓ 是 | — |

**总计**: 10 个阶段，47 个开发任务，Key P0 阻塞项已在各阶段标注。

---

## 2. 关键路径与并行策略

### 关键路径 (P0 → P4 → P8 → P9 → P10，共 6 个阶段)

```
P0 → P1 → P2 → P3 → P4 → P8 → P9 → P10
```

关键路径上的各阶段必须严格顺序执行，不可跳过。

### 并行策略

Phase 4 (Dense)、Phase 5 (Sparse)、Phase 6 (ColBERT)、Phase 7 (Fusion) 在 Phase 3 完成后可并行开发。如果团队有 3 名开发者，可一人负责一路索引，显著缩短工期。

### 质量门禁机制

每个阶段完成后必须通过质量门禁（测试全部通过）才能进入下一阶段。**不可跳过**，不可"批量实现后批量测试"——错误的 tokenizer 在 Phase 2 修复的代价远小于 Phase 8 才发现。

---

## 3. Phase 0: 项目脚手架与环境搭建

**目标**: 建立项目骨架、验证所有外部依赖可用、生成测试 Golden 文件。

### 任务清单

| ID | 任务 | 文件 | 描述 | 工作量 |
|----|------|------|------|--------|
| T1 | 初始化项目结构 | `cjpm.toml`, `src/` 所有子目录 | 创建 cjpm.toml（fountain 组织，依赖 f_config），创建所有源码子目录：backend/ tokenizer/ index/ fusion/ ffi/ platform/ config/。release profile 配置 lto + codegen-units=1。 | XS |
| T2 | 创建部署脚本 | `scripts/install_deps.sh`, `scripts/download_model.sh`, `scripts/convert_onnx.sh`, `scripts/verify_model.sh`, `Dockerfile`, `docker-compose.yml` | install_deps.sh: cmake/openblas/lapack 等系统依赖。download_model.sh: ModelScope 优先 + HuggingFace 回退。convert_onnx.sh: optimum-cli O2 优化导出。verify_model.sh: SHA256 + L2 norm 验证。Dockerfile: ubuntu:22.04 多阶段构建。 | S |
| T2a | 生成 Golden 文件 + 测试目录 | `scripts/gen_tokenizer_golden.py`, `scripts/gen_inference_golden.py`, `test/golden/`, `test/unit/`, `test/integration/`, `test/bench/` | 编写 Python 脚本生成 100 条文本 Tokenizer Golden（token IDs + attention_mask）和 7 条文本推理 Golden（dense/sparse/colbert 输出）。创建测试目录结构。**必须在 Phase 2 之前完成**。 | S |

### 质量门禁

| # | 门禁 | 阻塞条件 |
|---|------|---------|
| G0.1 | `cjpm build` | 编译错误 ≠ 0 |
| G0.2 | `NativeLibrary.load("onnxruntime")` | 返回 None |
| G0.3 | `NativeLibrary.load("faiss_c")` | 返回 None |
| G0.4 | `NativeLibrary.load("tokenizers")` | 返回 None |
| G0.5 | `HydraConfig.validate()` 对正确路径 | 返回非零错误 |
| G0.6 | Golden 文件已生成 | 文件不存在 |

### 风险

| 风险 | 等级 | 缓解 |
|------|------|------|
| ONNX Runtime .so 不在系统路径 | P0 | NativeLibrary 多路径搜索 + 环境变量 `fountain_hydra_onnx_lib_path` |
| HuggingFace Tokenizers C 无预编译包 | P0 | 提供 Rust cargo 编译脚本；Phase 0 验证失败则立即处理 |
| WSL 构建超时 | P3 | 文档记录预期 2-5 分钟构建时间 |

---

## 4. Phase 1: FFI 基础设施

**目标**: 声明所有 C 库的 FFI 类型和桥接函数，建立统一的错误检查框架。

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T3 | ONNX Runtime 不透明类型 | `src/ffi/onnx_types.cj` | 定义 `@C` 结构体：`OrtEnv`, `OrtSession`, `OrtSessionOptions`, `OrtRunOptions`, `OrtAllocator`, `OrtMemoryInfo`, `OrtValue`, `OrtStatus`。 | T1 | XS |
| T4 | Faiss 不透明类型 | `src/ffi/faiss_types.cj` | 定义 `@C` 结构体：`FaissIndex`。 | T1 | XS |
| T5 | ONNX Runtime C API 桥接 | `src/ffi/onnx_bridge.cj` | 声明所有 `@C foreign` 函数：OrtCreateEnv, OrtCreateSession, OrtCreateSessionOptions, OrtReleaseSessionOptions, OrtSetIntraOpNumThreads, OrtSetSessionGraphOptimizationLevel, OrtCreateSession, OrtGetAllocatorWithDefaultOptions, OrtCreateCpuMemoryInfo, OrtSessionGetInputCount/OutputCount, OrtSessionGetInputName/OutputName, OrtCreateTensorWithDataAsOrtValue, OrtRun, OrtGetTensorMutableData, Ortelease*。 | T3 | M |
| T6 | Faiss C API 桥接 | `src/ffi/faiss_bridge.cj` | 声明 `@C foreign` 函数：faiss_IndexHNSWFlat_new, faiss_IndexFlatL2_new, faiss_Index_add, faiss_Index_search, faiss_Index_train, faiss_write_index_fname, faiss_read_index_fname, faiss_Index_free。 | T4 | S |
| T7 | FFI 错误检查框架 | `src/ffi/ffi_check.cj` | 实现 `FfiCheck` 类：`checkOrtStatus(status, context)` — null=成功，非 null → 读错误消息 → release → throw。`checkFaissReturn(ret, context)` — ≥0=成功，<0→throw。`checkNotNull(ptr, context)` — 非 null 返回原值，null→throw。 | T3 | XS |
| T8 | 动态库加载器 | `src/platform/native_library.cj` | v1 Linux 版：搜索路径为 `fountain_hydra_lib_path` → /usr/local/lib → /usr/lib → /usr/lib/x86_64-linux-gnu。`normalizeLibraryName` 生成 Linux 后缀 `lib{name}.so`。 | T1 | S |

### 质量门禁

| # | 门禁 | 阻塞条件 |
|---|------|---------|
| G1.1 | 所有 FFI 桥接声明编译通过 | 编译错误 |
| G1.2 | `cjpm test --filter FfiCheckTest` 全部通过 | 测试失败 |
| G1.3 | `cjpm test --filter NativeLibraryTest` 全部通过 | 测试失败 |

### 测试覆盖

| 测试文件 | 测试项 |
|---------|--------|
| `ffi_check_test.cj` | checkOrtStatus(null) 不抛异常；非 null 抛异常含 context；checkFaissReturn(≥0) 不抛；checkFaissReturn(<0) 抛；checkNotNull(valid) 返回原值；checkNotNull(null) 抛 |
| `native_library_test.cj` | normalizeLibraryName 生成正确后缀；load(存在库) → Some；load(不存在库) → None；多路径搜索顺序正确 |

---

## 5. Phase 2: 配置、接口与 Tokenizer

**目标**: 定义核心类型和接口、实现 Tokenizer（共享组件）、配置校验。Tokenizer 必须在后端之前完成——分词不一致会导致 embedding 质量退化。

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T9 | TokenizeResult 结构体 | `src/tokenizer/tokenize_result.cj` | 字段：`inputIds: Array<Int64>`, `attnMask: Array<Int64>`, `seqLen: Int64`。 | T1 | XS |
| T10 | BgeTokenizer 实现 | `src/tokenizer/bge_tokenizer.cj` | **核心组件**。HuggingFace Tokenizers C FFI 封装。构造函数：加载 tokenizer.json，读取特殊 token ID。`tokenize(text)` → TokenizeResult。从 tokenizer_config.json 读取 CLS/PAD/SEP/UNK ID（不硬编码）。错误路径：文件不存在→抛异常含路径，文件损坏→抛异常。`close()` 释放 C 句柄。 | T7, T9 | M |
| T11 | BgeOutput 结构体 | `src/backend/bge_output.cj` | 字段：`denseVec: Array<Float32>` (1024)、`sparseVec: HashMap<Int64,Float32>`、`colbertVecs: Array<Array<Float32>>`。 | T1 | XS |
| T12 | BackendType 枚举 | `src/backend/backend_type.cj` | 构造器：ORT_CPU, ORT_CUDA, ORT_ROCM, ORT_OPENVINO, ORT_TENSORRT, MINDSPORE_LITE, COREML, HTTP_TEI, HTTP_VLLM。 | T1 | XS |
| T13 | BgeBackend 接口 | `src/backend/bge_backend.cj` | 方法：`encode()`, `encodeBatch()`, `warmup()`, `isReady()`, `backendInfo()`, `close()`。附带 `BackendCapabilities` 和 `BackendInfo` 结构体。 | T11, T12 | S |
| T14 | BackendConfig 结构体 | `src/backend/backend_config.cj` | 字段：`tokenizerPath`, `modelPath`, `numThreads`, `enablePerfLog`, `onnxLibPath?:String`。v1 平面结构。 | T1 | XS |
| T15 | ScoredDocument 结构体 | `src/fusion/scored_document.cj` | 字段：`docId: Int64`, `score: Float32`, `source: String`, `rank: Int64`。**共享类型，被所有 Indexer/Retriever/Fusion 消费**，提前到 Phase 2。 | T1 | XS |
| T16 | HydraConfig 配置 | `src/config/hydra_config.cj` | 所有 16+ 配置项的环境变量读取。`validate()` 方法：检查模型文件/分词文件存在性，参数范围校验（HNSW M、efConstruction、efSearch、ColBERT K、线程数、融合权重）。 | T1 (f_config 依赖) | M |
| T17 | Backend 工厂函数 | `src/backend/backend_factory.cj` | `createBgeBackend()` 从环境变量读取配置，按后端类型创建实例。v1 仅支持 ORT_CPU，未知类型降级为 CPU。 | T14 | S |

### 质量门禁

| # | 门禁 | 阻塞条件 |
|---|------|---------|
| G2.1 | BgeTokenizer golden 测试 **100% 匹配** Python transformers | 任一条文本 token IDs 不一致 |
| G2.2 | attention_mask 与 Python 一致 | 不匹配 |
| G2.3 | Tokenizer 错误路径：文件不存在 → 异常 | SIGSEGV |
| G2.4 | Tokenizer 错误路径：文件损坏 → 异常 | SIGSEGV |
| G2.5 | Tokenizer 资源生命周期：close 后操作 → 异常，重复 close 不 SIGSEGV | 崩溃 |
| G2.6 | BgeBackend 接口编译通过 | 编译错误 |
| G2.7 | `HydraConfig.validate()` 全部校验路径通过 | 测试失败 |

### Tokenizer Golden 测试（P0 阻塞）

**不可跳过**。Token IDs 必须与 Python `AutoTokenizer("BAAI/bge-m3")` 输出字节一致。

测试范围：100 条文本（英文/中文/混合/emoji/Unicode 特殊字符/RTL/空字符串/8000 字符长文本）。

---

## 6. Phase 3: OrtCpuBackend 实现

**目标**: 实现基于 ONNX Runtime CPU 的 BGE-M3 推理后端，包括零拷贝张量、真批量推理、按名称路由输出。

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T18 | 构造器 + 多步初始化 | `src/backend/ort_cpu_backend.cj` | Step 1-10: 创建 OrtEnv → SessionOptions → 设置线程数+图优化 → 创建 OrtSession → 缓存输入名称 → 按输出名建立 outputNameToIndex HashMap → 获取 Allocator+MemoryInfo → 创建 BgeTokenizer → 构建 BackendInfo → warmup()。**关键**：try-finally 异常路径资源清理——如果 session 创建失败，释放 opts；如果 tokenizer 失败，释放 session+env。 | T5, T7, T10, T13, T14 | L |
| T19 | 零拷贝张量工具函数 | `src/backend/ort_cpu_backend.cj` | `createInt64Tensor(data, shape)` — OrtCreateTensorWithDataAsOrtValue + checkOrtStatus + checkNotNull。`readFloatTensor(value, len)` — OrtGetTensorMutableData → Float32 数组。`getCount(fn)`, `getNames(fn, count)` — 输入/输出元数据。 | T18 | M |
| T20 | 单文本 encode() 实现 | `src/backend/ort_cpu_backend.cj` | Tokenize → createInt64Tensor(input_ids/attn_mask) → runInference（按名称路由，不依赖索引顺序）→ parseOutputs（按名称读取 dense_vec/sparse_vec/colbert_vec）→ 过滤特殊 token → 释放 OrtValue → 可选性能日志。`parseSparseOutput`：过滤 CLS/SEP/PAD/UNK，保留 weight>0 的 token。`reshapeColbert`：扁平张量 → Array<Array<Float32>>。 | T19 | M |
| T21 | 批量 encodeBatch() 实现 | `src/backend/ort_cpu_backend.cj` | Tokenize 全部文本 → 找到 maxSeqLen → 创建 [N, maxLen] 输入张量（Pad 用 0）→ 单次 OrtRun → 解析 N 条输出。空数组返回空；单元素委托 encode()。目标：比逐条循环快 2-5x。 | T20 | M |
| T22 | warmup/isReady/backendInfo/close | `src/backend/ort_cpu_backend.cj` | `warmup()`：encode("warmup") + 验证 Dense L2 norm ∈ [0.999, 1.001]。`isReady()`：session.isSome()。`backendInfo()`：返回缓存信息。`close()`：释放 session → release memInfo → release env → close tokenizer。幂等：反复 close 不崩溃。 | T20 | S |

### 质量门禁

| # | 门禁 | 阻塞条件 |
|---|------|---------|
| G3.1 | **7 条 Golden 文本 Dense vec FP32 全差 < 1e-6** | 误差 >= 1e-6 |
| G3.2 | **7 条 Golden 文本 Sparse vec tokenId→weight 映射完全一致** | 不一致 |
| G3.3 | **ONNX 输出按名称路由正确（故意乱序输出验证）** | 解析错误 |
| G3.4 | encodeBatch(1) == encode() 结果完全一致 | 不一致 |
| G3.5 | warmup 中 Dense L2 norm ∈ [0.999, 1.001] | 超范围 |
| G3.6 | 模型路径不存在 → 抛异常（非 SIGSEGV） | 崩溃 |
| G3.7 | 多步初始化中资源泄漏：模拟中间步骤失败，验证资源释放 | 泄漏 |
| G3.8 | init/close 循环 100 次 → 无内存增长 | 持续增长 |
| G3.9 | Tokenizer close 后 encode → 抛异常 | 静默执行 |

---

## 7. Phase 4: 稠密索引 (Dense + HNSW)

**目标**: 实现基于 Faiss HNSW 的稠密向量索引和检索器。Faiss 索引操作需 synchronized 保护。

**本阶段可与 Phase 5/6/7 并行开发。**

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T23 | DenseIndexer (HNSW) | `src/index/dense_indexer.cj` | 构造函数：faiss_IndexHNSWFlat_new(dim, M) + Mutex。`buildIndex(embeddings)`：扁平化数组，synchronized 内 faiss_Index_add。`buildIndexBatched(embeddings, batchSize=1000)`：分批构建避免双倍内存。`search(query, k)`：synchronized 内 faiss_Index_search，返回 Array<(docId, score)>。`save(path)`：faiss_write_index_fname。`close()`：faiss_Index_free。所有 Faiss 调用旁跟 FfiCheck。 | T6, T7 | L |
| T24 | DenseRetriever | `src/index/dense_retriever.cj` | 接收 DenseIndexer + timeout 配置。`search(queryVec, k, timeout)`：spawn 搜索在 Future，Future.get(timeout) → 超时返回空/部分结果（优雅降级）。 | T23, T15 | S |

### 质量门禁

| # | 门禁 |
|---|------|
| G4.1 | Auto-search: 向量自查询→第 1 结果为自己，距离 < 1e-4 |
| G4.2 | buildIndexBatched == buildIndex 搜索结果一致 |
| G4.3 | Save/Load 往返 → 搜索结果一致 |
| G4.4 | 空索引 search → 不崩溃 |
| G4.5 | Faiss add 失败 checkFaissReturn 生效 → 异常抛出 |
| G4.6 | 超时后返回部分结果（不 hang） |
| G4.7 | 并发 search 不 SIGSEGV（synchronized 生效） |

---

## 8. Phase 5: 稀疏索引 (Sparse + Inverted)

**目标**: 实现仓颉原生的倒排索引，支持跳跃指针和权重累加。只读 search 无需同步。

**本阶段可与 Phase 4/6/7 并行开发。**

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T25 | PostingList 数据结构 | `src/index/posting_list.cj` | `docIds: ArrayList<Int64>`, `weights: ArrayList<Float32>`, `skipPointers: ArrayList<Int64>`。`add(docId, weight)`, `buildSkipPointers()`（步长 sqrt(N)），`sortByDocId()`。 | T1 | S |
| T26 | SparseIndexer | `src/index/sparse_indexer.cj` | `buildIndex(sparseVecs)`：遍历文档→按 tokenId 创建/追加投递列表→排序+构建跳跃指针。`search(queryVec, k)`：拉取各 token 投递列表→合并+累加权重→TopK。 | T25, T15 | M |
| T27 | SparseRetriever | `src/index/sparse_retriever.cj` | 薄包装：`search(queryVec, k)` 委托 SparseIndexer。按文档权重降序返回 ScoredDocument 数组。 | T26 | XS |

### 质量门禁

| # | 门禁 |
|---|------|
| G5.1 | 单文档自查询 → 返回自己 |
| G5.2 | 多 token 查询 → 正确求和权重 |
| G5.3 | 跳跃指针构建正确 → 步长 = sqrt(N) |
| G5.4 | 查询不存在 token → 返回空（不崩溃） |
| G5.5 | 空索引/空查询 → 返回空 |
| G5.6 | 100 文档索引 + 全查询 → 性能可接受 |

---

## 9. Phase 6: 多向量索引 (ColBERT + IVF/PQ)

**目标**: 实现基于 Faiss IVF+PQ 的 ColBERT 多向量索引。训练与构建严格分离。

**本阶段可与 Phase 4/5/7 并行开发。**

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T28 | ColBERTIndexer | `src/index/colbert_indexer.cj` | **两阶段设计**：`train(tokenPool, K, M, subK)` — 创建 IndexFlatL2 量化器 → K-Means 训练 → PQ 子码本训练；增加进度日志。`buildIndex(docTokens)` — 验证已训练 → 找质心 → 残差 → PQ 编码 → 添加到 IVF 倒排。`search(queryTokens, k, centroidSearchWidth)` — MaxSim 检索。`save/load` 往返。所有 Faiss 调用 synchronized 保护。 | T6, T7, T15 | L |
| T29 | ColBERTRetriever | `src/index/colbert_retriever.cj` | 接收 ColBERTIndexer + timeout。`search(queryTokens, k, centroidSearchWidth, timeout)`：spawn MaxSim 在 Future → Future.get(timeout) → 超时返回部分结果。 | T28 | S |

### 质量门禁

| # | 门禁 |
|---|------|
| G6.1 | 训练 + 构建后 search 返回有效文档 |
| G6.2 | 未训练时 buildIndex → 抛 "must be trained before buildIndex" |
| G6.3 | Save/Load 往返 → 搜索结果一致 |
| G6.4 | centroidSearchWidth 影响结果数量 |
| G6.5 | 超时后返回部分结果（不 hang） |

---

## 10. Phase 7: 融合排序层

**目标**: 实现 RRF（默认）和加权融合两种策略。

**本阶段可与 Phase 4/5/6 并行开发。**

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T30 | FusionLayer | `src/fusion/fusion_layer.cj` | `fuseRRF(sparse, dense, colbert, topK)` — score(d)=Σ(1/(60+rank_r(d))) 各检索器，按 docId 去重合并。`fuseWeighted(scores, weights, topK)` — min-max 归一化后加权求和。`fuse(...)` 按策略字符串分发，未知策略降级 RRF。处理：空候选 → 返回空；TopK > 候选 → 返回全部；零/负权重 → 拒绝。 | T15 | M |

### 质量门禁

| # | 门禁 |
|---|------|
| G7.1 | RRF 公式计算：手动构造已知排名验证 1/(60+rank) 正确 |
| G7.2 | 出现在两路的文档 RRF 得分 > 仅出现在一路的文档 |
| G7.3 | Weighted α=1, β=0, γ=0 → 结果 = Dense 排序 |
| G7.4 | 未知策略 → 降级为 RRF（不崩溃） |
| G7.5 | 空候选集 → 返回空 |
| G7.6 | TopK 超限 → 返回全部候选 |

---

## 11. Phase 8: 引擎集成与并发安全

**目标**: 组装 HybridSearchEngine，实现完整检索流水线和正确的 close 生命周期。

### 任务清单

| ID | 任务 | 文件 | 描述 | 依赖 | 工作量 |
|----|------|------|------|------|--------|
| T31 | HybridSearchEngine 初始化 | `src/engine.cj` | 构造函数：HydraConfig.validate() → 失败掷异常；createBgeBackend() → 记 backendInfo()；创建三个 Indexer+FusionLayer；初始化 AtomicBool closing + activeFutures。 | T17, T16, T23, T26, T28, T30 | S |
| T32 | search + close 实现 | `src/engine.cj` | `search(query, topK)`：检查非 closing → encode → spawn 并行 dense+sparse 粗排 → Future.get(timeout) → ColBERT 精排 → Fusion → HybridSearchResult。`close()`：AtomicBool.closing=true → join activeFutures → embedder.close() → denseIndexer.close() → ... （先关资源后 join 避免死锁）。 | T31, T24, T27, T29 | M |

### 质量门禁

| # | 门禁 | 阻塞条件 |
|---|------|---------|
| G8.1 | search() 返回 ScoredDocument 数组 | 空/异常 |
| G8.2 | close() 后 search() → 抛 "engine is closing" | 静默执行 |
| **G8.3** | **2 线程并发 search() → 无 SIGSEGV 且结果正确** | **崩溃** |
| **G8.4** | **search() + close() 竞态 1000 次 → 0 SIGSEGV** | **崩溃** |
| G8.5 | **100 线程并发 search() → 全部完成** | 崩溃/hang |
| G8.6 | init/close 50 次循环 → FD 数不增 | 持续增长 |
| G8.7 | Config validate 所有错误路径覆盖 | 漏报/误报 |

**G8.3-G8.5 是本阶段的 P0 阻塞项。** 并发安全是架构审查 §15.2 的核心测试场景，必须在发布前通过。

---

## 12. Phase 9: 集成验证与性能基准

**目标**: 端到端验证、Python 交叉对比、内存安全审查、性能基线记录。

### 任务清单

| ID | 任务 | 描述 | 依赖 | 工作量 |
|----|------|------|------|--------|
| T33 | FFI Check 单元测试 | FfiCheck 三步函数所有成功/失败路径。 | T7 | S |
| T34 | Config 验证测试 | HydraConfig.validate() 全部边界值（M<4, M>128, efConstruction<10, numThreads>256, 融合权重数量/负数等） | T16 | S |
| T35 | Tokenizer Golden 测试 | 100 条文本与 Python AutoTokenizer 逐字节对比 | T10 | L |
| T36 | Backend encode 正确性测试 | 7 条文本与 Python ONNX Runtime 对比（FP32 < 1e-6） | T22 | XL |
| T37 | Backend encodeBatch 测试 | 可变长度文本、padding、性能提升比验证 | T21 | M |
| T38 | Dense 索引集成测试 | 构建 100 向量→自检索→save/load→并发访问 | T23 | M |
| T39 | Sparse 索引集成测试 | 多文档 multi-token 查询→排序验证 | T26 | M |
| T40 | ColBERT 索引集成测试 | Train→Build→Search 完整管线 | T28 | M |
| T41 | Fusion 融合测试 | RRF/Weighted 所有边界条件 | T30 | S |
| T42 | Engine 端到端测试 | buildIndex + search 全链路 + Python 交叉验证（Top10 ≥ 95% 匹配） | T32 | L |
| T43 | Engine 并发安全压测 | 100 线程 search + close 竞态 + init/close 循环 | T32 | M |
| T44 | 内存安全验证 | ASAN/Valgrind memcheck 对 encode+search 路径；Valgrind helgrind 对并发测试 | T32 | M |
| T45 | 性能基准测试 | 7 项 `cjpm bench`：encode 延迟、encodeBatch 加速比、buildIndex 吞吐、Dense/Sparse/ColBERT search 延迟、Fusion 延迟、E2E 延迟 | T32 | M |

### 质量门禁

| # | 门禁 | 阻塞条件 |
|---|------|---------|
| G9.1 | 全部 42 个测试通过 | 任一失败 |
| G9.2 | Python Top10 ≥ 95% 匹配 | < 95% |
| G9.3 | ASAN clean（我方代码） | 报错 |
| G9.4 | Valgrind memcheck 0 "definitely lost" | 非零 |
| G9.5 | 并发测试 100% 通过（无 SIGSEGV） | 崩溃 |
| G9.6 | 7 项基准测试可运行并记录基线 | 报错 |

---

## 13. Phase 10: 发布准备

**目标**: 文档完善、安装脚本验证、发布包检查。

### 任务清单

| ID | 任务 | 描述 | 工作量 |
|----|------|------|--------|
| T46 | README.md 快速开始 | 5 分钟快速开始流程：cjpm add → 配置 3 个环境变量 → search()。附带常见问题 FAQ。 | S |
| T47 | 安装脚本集成测试 | 干净 Linux VM 执行 install_deps → download_model → cjpm build → cjpm test 全部通过。 | M |
| T48 | Docker 镜像构建 + 冒烟测试 | docker build + docker run → search 返回结果。 | S |
| T49 | Release 检查 | 版本号一致（cjpm.toml / 代码 / 文档）、LICENSE 第三方兼容性检查、`cjpm publish --dry-run` 无错误。 | S |

### 质量门禁

| # | 门禁 |
|---|------|
| G10.1 | README 快速开始流程可复现 |
| G10.2 | 安装脚本在干净环境成功 |
| G10.3 | Docker 镜像构建+冒烟测试通过 |
| G10.4 | cjpm publish --dry-run 无错误 |
| G10.5 | LICENSE 合规（ONNX Runtime MIT / Faiss MIT / Tokenizers Apache-2.0） |

---

## 14. 依赖关系图

```
T1 (项目骨架)
├─► T2  (部署脚本) ─► T2a (Golden 文件)
├─► T3  (onnx_types)   ─► T5  (onnx_bridge)
│                         ├─► T7  (ffi_check) ─► T10 (Tokenizer)
│                         │                      └─► T18-T22 (OrtCpuBackend)
│                         │                           └─► T17 (factory) ─► T31 (engine init)
│                         └─► T18 (OrtCpuBackend)
├─► T4  (faiss_types)   ─► T6  (faiss_bridge) ─► T23 (DenseIndexer)
│                                                 └─► T24 (DenseRetriever)
│                                              ─► T28 (ColBERTIndexer)
│                                                 └─► T29 (ColBERTRetriever)
├─► T9  (TokenizeResult) ─► T10 (Tokenizer) ─► T18
├─► T11 (BgeOutput) ─► T13 (BgeBackend) ─► T18
├─► T12 (BackendType) ─► T13
├─► T14 (BackendConfig) ─► T17 (factory)
├─► T15 (ScoredDocument) ─► T24/T26/T27/T28/T29/T30
├─► T16 (HydraConfig) ─► T31
├─► T25 (PostingList) ─► T26 (SparseIndexer) ─► T27 (SparseRetriever)
├─► T30 (FusionLayer) ─► T31

T31 (engine init) ─► T32 (engine search/close) ─► T33-T45 (tests) ─► T46-T49 (release)
```

**关键路径** (最长链)：
```
T1 → T3 → T5 → T10 → T18 → T20 → T36 (encode golden) → T42 (E2E) → T49 (release)
```

---

## 15. 风险矩阵

| # | 风险 | 等级 | 检测阶段 | 缓解 |
|---|------|------|---------|------|
| 1 | **Tokenizer 输出与 Python 不一致** | **P0** | P2 | Golden 100 条文本 100% 匹配 → 否则 STOP |
| 2 | **FFI 错误检查遗漏** | **P0** | P1 | FfiCheck 框架 + grep 扫描所有 FFI 调用点 |
| 3 | **零拷贝内存安全** | **P0** | P9 | ASAN/Valgrind 验证；降级方案：C 侧拷贝 |
| 4 | **ONNX 输出顺序不可靠** | **P0** | P3 | 名称路由 (outputNameToIndex)，故意乱序测试 |
| 5 | **search() + close() 竞态 SIGSEGV** | **P0** | P8 | AtomicBool + join futures + 关闭顺序 |
| 6 | Dense L2 norm ≠ 1.0 | P1 | P3 | warmup 验证；不合格自动归一化 |
| 7 | Faiss 索引非线程安全 | P1 | P8 | synchronized(lock) 保护所有 add/search |
| 8 | ColBERT 未训练即构建 | P1 | P6 | 抛 "must be trained before buildIndex" |
| 9 | 多步初始化资源泄漏 | P1 | P3 | try-finally + 故障注入测试 |
| 10 | ONNX Runtime 版本兼容 | P1 | P0 | v1.17.0 锁定 + NativeLibrary 多路径搜索 |
| 11 | HuggingFace Tokenizers C 库打包 | P1 | P0 | Rust cargo 编译脚本 + 预编译 .so 下载 |
| 12 | ColBERT 训练耗时 | P2 | P6 | 采样训练 + 进度日志 + 测试用小型 token pool |
| 13 | encodeBatch pad 影响推理 | P2 | P3 | batch 单条 vs 独立 encode 对比测试 |
| 14 | WSL 构建超时 | P3 | P0 | 文档记录 2-5 分钟预期 |
| 15 | LLVM LTO Broken Module | P3 | All | 已知间歇性问题，重试 |

---

**计划完成**。本文档经 3 名架构师两轮独立脑暴 + 交叉评审生成，覆盖 49 个开发任务、10 个阶段、完整依赖图和 15 项风险矩阵。每阶段设质量门禁，关键 P0 阻塞项明确标注。

package fountain::f_hydra

// Faiss v1.9.0 C API opaque structure type for FFI.
// All index types (HNSW, IVF, Flat, PQ) share this single handle.

@C
public class FaissIndex {
    // opaque handle
}

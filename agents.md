1. 仓颉工程指南
- 仓颉语言
   * 语法问题使用cangjie_docs 相关工具在文档中查找，不要猜API和语法
   * 在提示语法错误时使用cangjie-mem加载语言级记忆match的case后不能接{}，case后直接写多行表达式有固定结构的类不要使用jsonvalue手动解析，实现json.stream的序列化接口
  * 除非实在没固定结构的类，否则禁止使用jsonvalue动态获取

2. 仓颉语言
- 不要考虑时间，不要简化算法，不要简化测试，这项目是自己的产品，按最好的来搞
- 新功能必须写单元测试，以仓颉单元测试为主
- 测试发现的新问题需要解决，且要添加新用例到单元测试里有单元测试后再提交，仓颉的所有项目都建议使用TDD模式不要在项目外创建仓颉单文件测试，非cjpm项目没法导入当前项目

3. 任务指南

4. 忆时塔(chronomem)工作日志
5. Git分支流程
- 开发一律开功能分支，不在master 上直接开发:git checkout -b feature/<feature-name>
- 分支命名:feature/<功能名>(新功能)/fix/缺陷名>(修复)功能完成后:合并回master，并删除已合并的分支
- 每个任务完成后独立提交(遵循「有单元测试后再提交」)，允许分支内多次提交
- 合并方式:优先git merge --no-ff(保留分支历史)
- 每次开工前先git pull同步远程
6. 常用命令
7. LSP报错
8. 相关技能(skills)
9. 网站相关
